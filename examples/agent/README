The file 'INFILE.yaml' will produce a graph with follow by agent as the follow mechanism. To use just copy the file into the previous directory as INFILE.yaml and it will be read into the simulation.
