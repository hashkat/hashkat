# See DEFAULT.yaml for full configuration options and documentation!

analysis:
  initial_entities:
    100
  max_entities: 
    100001
  max_time: 
    hour
  max_real_time: 
    hour
  enable_interactive_mode:
    true
  use_barabasi: 
    true 
  use_random_time_increment: 
    true
  use_followback: 
    true
  follow_model: # See notes above
    twitter
  # model weights ONLY necessary for follow method 'twitter'    
  model_weights: {random: 0.01, preferential: 0.99, entity: 0.00, preferential_entity: 0.0, hashtag: 0.00}
  
  stage1_unfollow: 
    true
  unfollow_tweet_rate: 
    10
  use_hashtag_probability:
    1

output:
  save_network_on_timeout: 
    false
  load_network_on_startup:
    false
  ignore_load_config_check: # Whether to abort loading configuration because of (strictly) mismatching configuration
    false
  save_file: # File to save to, and load from
    network_state.sav

